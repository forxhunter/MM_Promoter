name: mm_promoter
channels:
  - defaults
dependencies:
  - python=3.9
  - numpy>=1.19
  - scipy>=1.5
  - pandas>=1.1
  - matplotlib>=3.3
  - seaborn>=0.11
  - h5py>=2.10
  - tqdm
  - pip
  - pip:
    - scienceplots
    - latticemicrobes  # If available via pip, otherwise install manually
    - jupyterlab
